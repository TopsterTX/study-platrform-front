import { CenterContainer } from "@/shared";

export default function Loading() {
  return (
    <CenterContainer
      style={{
        height: "100svh",
      }}
    >
      Loading...
    </CenterContainer>
  );
}
